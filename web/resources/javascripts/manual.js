document.addEventListener('DOMContentLoaded', function () {
    // Event listener code and carousel initialization here
    // Example:
    const certificatenextButton = document.getElementById('certificatenextButton');
    certificatenextButton.addEventListener('click', function () {
        // Handle certificatenext button click
    });

    // Initialize the Embla Carousel and other related code
});
